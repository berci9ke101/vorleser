apiVersion: apps/v1
kind: Deployment
metadata:
  name: vorleser-elefant
spec:
  selector:
    matchLabels:
      app: vorleser-elefant
  template:
    metadata:
      labels:
        app: vorleser-elefant
    spec:
      containers:
      - name: postgres
        image: centos/postgresql-13-centos7
        env:
        - name: POSTGRESQL_USER
          value: "vorleser_user"
        - name: POSTGRESQL_PASSWORD
          value: "secretpassword"
        - name: POSTGRESQL_DATABASE
          value: "vorleser_db"
        ports:
        - containerPort: 5432
        volumeMounts:
        - name: vorleser-elefant-data
          mountPath: /var/lib/pgsql/data
      volumes:
      - name: vorleser-elefant-data
        persistentVolumeClaim:
          claimName: postgres-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: vorleser-elefant
spec:
  ports:
  - port: 5432
  selector:
    app: vorleser-elefant